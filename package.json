{
  "name": "newtripoli-mcp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "node generate.mjs",
    "deploy": "node generate.mjs && npx wrangler deploy",
    "freeze-goldens": "node scripts/freeze-goldens.mjs",
    "check": "node scripts/check-canon-sync.mjs && node scripts/check-kernel-digest.mjs && node scripts/check-goldens.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "fetch-to-node": "2.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
